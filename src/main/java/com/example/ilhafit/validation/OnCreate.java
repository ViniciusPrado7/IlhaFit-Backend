package com.example.ilhafit.validation;

/**
 * Grupo de validacao aplicado apenas no cadastro (criacao).
 * Constraints marcadas com este grupo nao sao avaliadas na atualizacao,
 * permitindo, por exemplo, que a senha seja obrigatoria so no cadastro.
 */
public interface OnCreate {
}
